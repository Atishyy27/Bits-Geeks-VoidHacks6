name: locsender
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^9.0.0
  http: ^1.2.2  # Update to the latest version
  cupertino_icons: ^1.0.8
  permission_handler: ^10.2.0  # Corrected indentation
  location: ^4.4.0  # Add location package dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
